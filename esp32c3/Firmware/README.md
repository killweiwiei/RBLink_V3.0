# ESP32-C3 release artifacts

Flash offsets from `flasher_args.json`:

- `bootloader.bin` at `0x0000`;
- `partition-table.bin` at `0x8000`;
- `rblink_esp32c3.bin` at `0x10000`.

The image targets ESP32-C3-MINI-1-N4/N4X with 4 MB flash.
